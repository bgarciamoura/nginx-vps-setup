# Configurações Gerais
PROJECT_NAME=nginx-vps
COMPOSE_PROJECT_NAME=nginx-vps

# Email para notificações SSL (Let's Encrypt)
SSL_EMAIL=seu-email@dominio.com

# Domínios (separados por espaço)
# Exemplo: DOMAINS="app1.dominio.com app2.dominio.com api.dominio.com"
DOMAINS=""

# Configurações do Nginx
NGINX_WORKER_PROCESSES=auto
NGINX_WORKER_CONNECTIONS=2048
NGINX_CLIENT_MAX_BODY_SIZE=100M

# Rate Limiting
RATE_LIMIT_GENERAL=10r/s
RATE_LIMIT_API=30r/s
RATE_LIMIT_AUTH=5r/m

# Timezone
TZ=America/Sao_Paulo

# Configurações de Backup
BACKUP_RETENTION_DAYS=30

# Ambiente (production, staging, development)
ENVIRONMENT=production

# Recursos Docker (opcional - limitar se VPS pequena)
# NGINX_MEMORY_LIMIT=512M
# NGINX_CPU_LIMIT=0.5
